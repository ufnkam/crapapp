[package]
name = "example"
version = "0.2.0"
edition = "2024"

[features]
some_feature = []

[dependencies]
clap = { version = "4.5.40", features = ["derive"] }

[build-dependencies]
cargo-crapapp = { path = "..", default-features = false, features = ["windows"] }
